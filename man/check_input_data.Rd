% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_checks.R
\name{check_input_data}
\alias{check_input_data}
\title{Check input data for required columns and data types}
\usage{
check_input_data(data)
}
\arguments{
\item{data}{A data frame containing the input data.}
}
\value{
Error messages if any issues are found. If no issues are found,
        it returns TRUE.
}
\description{
This function checks if a data frame contains all the necessary columns and
if the columns have the correct data types.
}
\examples{
\dontrun{
data <- read.csv("../data/input/input.csv")
result <- check_input_data(data)
if (is.logical(result) && result == TRUE) {
  print("Data is valid.")
} else {
  cat(as.character(result), sep = "\n")
}
}
}
