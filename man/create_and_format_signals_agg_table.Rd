% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/results_table.R
\name{create_and_format_signals_agg_table}
\alias{create_and_format_signals_agg_table}
\title{Create and format the aggregated signal results table for one category and orders the strata by the factor levels}
\usage{
create_and_format_signals_agg_table(signals_agg, interactive = TRUE)
}
\arguments{
\item{signals_agg}{A tibble or data.frame containing aggregated signals produced from \code{\link{aggregate_signals(signals,number_of_weeks = 6)}}}

\item{interactive}{Logical indicating whether to create an interactive
DataTable (default is TRUE).}
}
\value{
An interactive DataTable or a static gt table, depending on the value
  of `interactive`.
  #' @examples
\dontrun{
signals_agg <- input_example %>%
preprocess_data() %>%
get_signals(stratification = c("age_group","sex")) %>%
aggregate_signals(number_of_weeks = 6) %>%
filter(category == "age_group")

signals_agg_processed <- process_signals_agg_for_table(signals_agg)
create_and_format_signals_agg_table(signals_agg_processed)
}
}
\description{
This function combines the preprocessing of the aggregated signals data.frame with the final formating of the table by applying \code{\link{process_signals_agg_for_table()}} and \code{\link{format_table()}}.
}
