% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_signals.R
\name{get_strata_from_signals_agg}
\alias{get_strata_from_signals_agg}
\title{Extract strata from precomputed signals_agg}
\usage{
get_strata_from_signals_agg(signals_agg)
}
\arguments{
\item{signals_agg}{A data frame containing precomputed signal aggregations, produced by [aggregate_signals()].
Must include a `category` column that encodes the stratification variable(s).}
}
\value{
A character vector of stratification values (e.g., `"age_group"`, `"county"`),
  or `NULL` if no stratification was applied.
}
\description{
This helper function returns the list of stratification variables used
to generate a `signals_agg` object. If no stratification was applied
(i.e., all values in the `category` column are `NA`), it returns `NULL`.
}
