# check whether there is a completely empty row in provided surveillance data

check whether there is a completely empty row in provided surveillance
data

## Usage

``` r
check_empty_rows(data)
```

## Arguments

- data:

  data.frame, raw linelist of surveillance cases

## Value

boolean, TRUE when there was an empty row inside the data, FALSE when no
empty rows
