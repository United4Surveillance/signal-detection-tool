# Removing columns from data which only contain missing values

Removing columns from data which only contain missing values

## Usage

``` r
remove_empty_columns(data)
```

## Arguments

- data:

  data.frame, dataset to remove empty columns from, can be linelist of
  surveillance data

## Value

data.frame without columns which only contained missing values
