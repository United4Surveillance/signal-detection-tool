# Retrieveing which columns in the dataset only contain missing values

Retrieveing which columns in the dataset only contain missing values

## Usage

``` r
get_empty_columns(data)
```

## Arguments

- data:

  data.frame, dataset to check for empty columns can be linelist of
  surveillance data

## Value

named vector with column names and boolean specifying complete
missingness or not
